---
name: i18n-template
applies-to: "**/app/**/*.tsx, **/app/**/*.ts, **/pages/**/*.tsx, **/pages/**/*.ts"
description: Template for feature and global translations
when-to-use: adding translations, i18n setup, localization
keywords: i18n, translations, localization, next-intl, messages
priority: medium
related: architecture-patterns.md
---

# i18n Template

## Feature Translations

Location: `modules/[feature]/src/i18n/`

### English (en.json)

```json
{
  "title": "Feature Title",
  "description": "Feature description text",
  "actions": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete"
  },
  "errors": {
    "required": "This field is required",
    "invalid": "Invalid value"
  },
  "success": {
    "saved": "Successfully saved",
    "deleted": "Successfully deleted"
  }
}
```

### French (fr.json)

```json
{
  "title": "Titre de la fonctionnalité",
  "description": "Description de la fonctionnalité",
  "actions": {
    "save": "Enregistrer",
    "cancel": "Annuler",
    "delete": "Supprimer"
  },
  "errors": {
    "required": "Ce champ est requis",
    "invalid": "Valeur invalide"
  },
  "success": {
    "saved": "Enregistré avec succès",
    "deleted": "Supprimé avec succès"
  }
}
```

---

## Global Translations

Location: `modules/cores/i18n/`

### Structure

```
modules/cores/i18n/
├── en.json           # English global
├── fr.json           # French global
└── index.ts          # Export helper
```

### Global English (en.json)

```json
{
  "common": {
    "loading": "Loading...",
    "error": "An error occurred",
    "retry": "Retry",
    "close": "Close"
  },
  "navigation": {
    "home": "Home",
    "dashboard": "Dashboard",
    "settings": "Settings",
    "logout": "Logout"
  },
  "validation": {
    "required": "Required",
    "email": "Invalid email",
    "minLength": "Minimum {min} characters",
    "maxLength": "Maximum {max} characters"
  }
}
```

---

## Usage with next-intl

### In Server Component

```typescript
import { getTranslations } from 'next-intl/server'

export default async function FeaturePage() {
  const t = await getTranslations('feature')

  return <h1>{t('title')}</h1>
}
```

### In Client Component

```typescript
'use client'

import { useTranslations } from 'next-intl'

export function FeatureForm() {
  const t = useTranslations('feature')

  return <button>{t('actions.save')}</button>
}
```

---

## Naming Conventions

| Key Pattern | Usage |
|-------------|-------|
| `title` | Page/section title |
| `description` | Descriptive text |
| `actions.*` | Button/action labels |
| `errors.*` | Error messages |
| `success.*` | Success messages |
| `labels.*` | Form labels |
| `placeholders.*` | Input placeholders |
