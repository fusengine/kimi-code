---
name: 03.5-elicit
description: React expert self-review using elicitation techniques
prev_step: references/react/03-execution.md
next_step: references/react/04-validation.md
---

# 03.5-elicit - React Expert Self-Review

React expert self-reviews code using elicitation techniques before sniper validation.

---

## React Specific Techniques

### Auto-Detection for React Code

| Code Type | Auto-Selected Techniques |
|-----------|--------------------------|
| Components | ARCH-01 (SOLID), UX-01 (a11y), ARCH-04 (Size) |
| Hooks | TEST-01 (Edge), CONC-01 (Race), MAINT-01 (Readability) |
| State (Zustand) | ARCH-02 (Dependency), TEST-02 (Error), DATA-03 (Sync) |
| Forms (TanStack) | SEC-02 (Input), UX-02 (Error), TEST-03 (Boundary) |
| Router (TanStack) | INT-01 (Contract), UX-03 (Loading), TEST-01 (Edge) |
| API Calls | SEC-01 (Auth), PERF-05 (Cache), INT-05 (Timeout) |

---

## React Priority Checks

### 1. Component Architecture
```text
□ Single responsibility (one purpose per component)
□ Props interface extracted to src/interfaces/
□ No prop drilling (use context or Zustand)
□ Memoization where needed (React.memo, useMemo)
```

### 2. Hook Rules
```text
□ No hooks in conditions
□ No hooks in loops
□ Custom hooks extract reusable logic
□ useEffect cleanup functions
```

### 3. State Management
```text
□ Local state for UI-only
□ Zustand for shared state
□ TanStack Query for server state
□ No redundant state (derived values)
```

### 4. Accessibility
```text
□ Semantic HTML elements
□ ARIA labels where needed
□ Keyboard navigation
□ Focus management
```

---

## Technique Quick Reference (React)

| ID | Technique | React Focus |
|----|-----------|-------------|
| ARCH-01 | SOLID | Component single responsibility |
| ARCH-04 | File Size | <100 lines per component |
| UX-01 | Accessibility | ARIA, semantic HTML, keyboard |
| UX-02 | Error Messages | Form validation feedback |
| UX-03 | Loading States | Suspense, loading indicators |
| TEST-01 | Edge Cases | Empty, loading, error states |
| CONC-01 | Race Conditions | useEffect cleanup, AbortController |
| PERF-04 | Bundle Size | Dynamic imports, tree shaking |
| MAINT-01 | Readability | Clear naming, simple logic |
| SEC-02 | Input Validation | Form validation with Zod |

---

## Example: Component Review

```tsx
// Before elicitation
function UserList({ users }) {
  return (
    <div>
      {users.map(u => <div>{u.name}</div>)}
    </div>
  )
}

// After UX-01 + TEST-01 + ARCH-01
interface UserListProps {
  users: User[]
}

function UserList({ users }: UserListProps) {
  if (users.length === 0) {
    return <p role="status">No users found</p>
  }

  return (
    <ul aria-label="User list">
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  )
}
```

---

## Output Format

```text
## 🟣 React Elicitation Report

Framework: React 19
Mode: auto
Code Type: Component

### Applied Techniques
1. UX-01: Accessibility → Added ARIA labels
2. TEST-01: Edge Cases → Added empty state
3. ARCH-01: SOLID → Extracted interface

### Corrections Made
- Changed div to semantic ul/li
- Added aria-label for screen readers
- Added empty state handling
- Extracted UserListProps interface

→ Ready for 04-validation (sniper)
```

---

## Update Task Phase

At the **start** of this phase, record it in `.kimi/apex/task.json`:

```bash
jq --arg p "elicit" '.tasks[.current_task].phase = $p' .kimi/apex/task.json \
  > .kimi/apex/task.json.tmp && mv .kimi/apex/task.json.tmp .kimi/apex/task.json
```

---

## Next Step

→ `04-validation.md`: sniper with React linters (Biome, TypeScript)
